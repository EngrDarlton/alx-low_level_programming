#include <stdio.h>
#include <unistd.h>

/**
 * main - Entry point
 * Description: Print 1  to 10
 * Return: 0 Success
*/

int main(void)
{
	int a;

	for (c = 0; c < 10; c++)
	{
		printf("%d", c);
	}
	putchar('\n');

	return (0);
}
